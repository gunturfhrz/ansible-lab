---
- hosts: localhost
  gather_facts: false

  tasks:

    - name: Get namespaces
      kubernetes.core.k8s_info:
        api_version: v1
        kind: Namespace
      register: ns

    - name: Show namespaces
      debug:
        msg: "{{ ns.resources | map(attribute='metadata.name') | list }}"