---
- hosts: localhost
  gather_facts: false

  tasks:
    - name: Apply deployment
      kubernetes.core.k8s:
        state: present
        src: manifests/deployment.yaml

    - name: Apply service
      kubernetes.core.k8s:
        state: present
        src: manifests/service.yaml