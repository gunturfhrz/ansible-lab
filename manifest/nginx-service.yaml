- name: Create service
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: v1
      kind: Service
      metadata:
        name: nginx-awx
        namespace: awx-lab
      spec:
        selector:
          app: nginx-awx
        ports:
          - port: 80
            targetPort: 80